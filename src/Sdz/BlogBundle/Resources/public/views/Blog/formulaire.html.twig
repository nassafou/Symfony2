{# Cette vue n'hérite de personne #}
{% import 'form.html' as forms %}

<h3> Formulaire d'article </h3>
{# Ici on laisse vide la vue pour l'instant, on la comblera plus
 tard lorsqu'on saura afficher un formulaire #}
 <div class="well">
    Ici se trouve le formulaire
    <dl>
      <dt> Nom</dt>
      <dd>{{forms.input('Nom')}}</dd>
      <dt> Email </dt>
      <dd>{{ forms.input('Email', none,'email' ) }}</dd>
      <dt>Mot de passe</dt>
      <dd>{{forms.input('Password', none,'password')}}</dd>
    </dl>
 </div>